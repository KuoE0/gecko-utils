source "$BUILDWITH_HOME/common"

ac_add_options --enable-optimize

mk_add_options AUTOCLOBBER=1
mk_add_options MOZ_MAKE_FLAGS="-s -j${CPU}"
mk_add_options MOZ_OBJDIR=@TOPSRCDIR@/../obj-${BRANCH}-${OS}-${CONFIG}
